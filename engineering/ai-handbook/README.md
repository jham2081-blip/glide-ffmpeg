# Vendored AI Engineering Handbook

This directory is the versioned `software-standard` snapshot from `jham2081-blip/ai-agent-skills`, pinned by `HANDBOOK_ADOPTION.json`.

Glide's root `AGENTS.md`, current code/tests, benchmark methodology, and public API remain authoritative. Load only the handbook skill or GPT-6 field guide relevant to the task.
